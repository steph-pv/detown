def talktoLucky():
    if unscrambled_eggs = True:
        receiveOrder()
    else:
        print("...")

def receiveOrder():
    # inv class
    # pass Order to inv class to create an inv object
    #add to inventory function
    print("ORDER added to Inventory!")
    # order desc: wax sealed and pristine

def buy():
    # buy class
    # pass markets/items to buy class to facilitate sales
    print(f"{item} added to inventory.")

def sell():
    # sell class
    # pass items to sell class to faciliate sale, update inv, and add to money
    print(f"{item} sold for {amount}")

def vendors():
    # vendorList = list of vendors and description
    # vendor class w tiers
    # marketgo method for vendor class
    i = input("Where would you like to go?")
    if i.lower() in vendorList:
        marketgo(vendor)